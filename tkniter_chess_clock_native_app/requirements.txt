# Tkinter typically ships with Python in standard distributions.
# Add common optional dependencies used by Tk UI apps.
# The startup script tolerates installation failures or a missing file.
pillow>=9.0.0
